# Dataset Pipeline Spec

本文档定义 `nilm` 仓库的通用数据集处理规范，覆盖输入端、检测端和修复端，目标是将不同来源的 NILM 原始数据统一转换为可训练、可复现、可审计的数据资产。本文档面向“可接入任意新数据集”的通用 pipeline 设计，不以任何已知数据集名称、文件组织方式或历史实现为边界。

## 1. 目标

统一后的数据流水线应满足以下目标：

1. 不同数据集以统一准入标准进入仓库。
2. 原始数据先按房屋和电器通道拆分，再进入质量检测。
3. 检测结果可追踪，可输出问题清单和统计摘要。
4. 修复策略明确、可重复执行，避免手工临时清洗。
5. 最终输出满足当前训练代码的读取要求。

---

## 2. 流水线总览

```text
原始数据（任意 NILM 数据集）
        ↓
输入端（准入校验）
        ↓
DataSeparator
        ↓
分离阶段的中间标准信号文件
        ↓
单文件检测（结构检测 + 时间检测 + 数值检测 + 映射检测）
        ↓
单文件修复（重采样 + 缺失修复 + 异常裁剪 + 状态修复）
        ↓
时间对齐与最终导出
        ↓
训练就绪数据（CSV 标准化导出）
```

分离阶段中间输出形态示例：

```text
<save_path>/<DATASET>_data_separated/
  ├── House_1/
  │   ├── aggregate_H1.h5
  │   ├── dishwasher_H1.h5
  │   ├── kettle_H1.h5
  │   └── ...
  ├── House_2/
  │   ├── aggregate_H2.h5
  │   └── ...
  └── ...
```

最终训练输出形态示例：

```text
<save_path>/<DATASET>_dataset/
  ├── dishwasher_H1.csv
  ├── dishwasher_H2.csv
  ├── dishwasher_H5.csv
  ├── fridge_H1.csv
  ├── fridge_H2.csv
  ├── kettle_H1.csv
  ├── kettle_H2.csv
  ├── kettle_H3.csv
  ├── kettle_H5.csv
  └── ...
```

这里要区分两类输出：

1. 分离阶段中间输出：`House_x/*.h5`
2. 最终训练输出：`<appliance>_Hx.csv`

前者用于承接原始数据分离、质量检测和修复流程，后者用于给训练代码直接读取。

---

## 3. 输入端规范

### 3.1 适用范围

本规范适用于任意满足 NILM 基本语义的数据集。

### 3.2 数据准入标准

#### 3.2.1 层级结构要求

准入数据集必须满足如下层级语义：

```text
dataset.h5/
└── House_<house_id>/
    ├── aggregate
    ├── <appliance_name_1>
    ├── <appliance_name_2>
    └── ...
```
也就是说，数据集中必须能够明确区分：

1. 不同 house
2. 每个 house 下的 aggregate 信号
3. 每个 house 下的 appliance 信号

可以是目录结构、文件名结构或 HDF5 内部层级，但最终必须能表达上述语义。

#### 3.2.2 字段名要求

准入数据中的标准字段名规定如下：

- 时间字段：`time`
- 总表字段：`aggregate`
- 电器字段：`<appliance_name>`

其中：

1. `time` 表示时间戳
2. `aggregate` 表示总功率
3. `<appliance_name>` 使用小写字母和下划线命名，例如：
   - `fridge`
   - `dishwasher`
   - `washing_machine`

房屋目录和文件命名规范：

- 目录：`House_<house_id>/`
- 总表：`aggregate`
- 电器表：`<appliance_name>`

#### 3.2.3 字段格式要求

除字段名外，准入数据中的字段格式还应满足以下要求：

1. `time` 字段
   - 必须可解析为合法时间戳
   - 推荐使用 ISO 8601 格式，例如 `2024-01-01 12:30:00`
   - 同一数据集内时间格式必须一致
2. `aggregate` 字段
   - 必须为数值型
   - 表示总功率
   - 单位应统一，推荐为瓦特 `W`
3. `<appliance_name>` 字段
   - 必须为数值型
   - 表示对应电器功率
   - 单位应与 `aggregate` 保持一致
4. 所有功率字段
   - 不应混用字符串、布尔值或其他非数值格式
   - 小数和整数均可，但同一列应保持可稳定解析

#### 3.2.4 采样频率规范

准入数据集的采样频率应不低于 `1 kHz`。

输入端只接受满足以上层级结构、字段命名、字段格式和采样频率要求的数据集。

---

## 4. 分离端

输入端通过后，原始数据进入分离阶段。

### 4.1 分离目标

分离端的目标是将原始数据拆分为“每户一目录、每个信号一个标准文件”的中间格式。

### 4.2 输出格式

分离端输出的是中间产物，不是最终训练输出。

标准输出形态规定如下：

```text
<save_path>/<DATASET>_data_separated/
  ├── House_1/
  │   ├── aggregate_H1.h5
  │   ├── dishwasher_H1.h5
  │   ├── kettle_H1.h5
  │   └── ...
  ├── House_2/
  │   ├── aggregate_H2.h5
  │   └── ...
  └── ...
```

例如：

```text
House_1/
  ├── aggregate_H1.h5
  ├── dishwasher_H1.h5
  ├── kettle_H1.h5
```

其中表内容示例：

| time | aggregate |
|------|-----------|
| 2013-05-17 09:35:12 | 223.45 |
| 2013-05-17 09:35:18 | 225.12 |

### 4.3 字段名与字段格式要求

分离端输出文件中的字段要求如下：

1. 时间字段统一命名为 `time`
2. 功率字段名必须与文件名中的信号名一致
3. `time` 字段必须可解析为合法时间戳
4. 功率字段必须为数值型
5. 同一文件中只允许一个功率字段

### 4.4 文件与存储格式要求

分离端输出文件的保存要求如下：

1. 文件后缀统一为 `.h5`
2. HDF5 key 统一为 `dataset`
3. 每个文件只保存一个信号
4. 文件名格式统一为 `<signal_name>_H<house_id>.h5`

### 4.5 Aggregate 规则

对总表的定义如下：

- 若原始数据存在单一 mains/aggregate 通道，则直接使用。
- 若原始数据有多路 mains，则允许合成 aggregate。
- 若原始数据没有显式 aggregate，但可由多个总表信号推导，也允许推导生成。
- 若原始数据既无总表也无法推导，则该数据集不满足当前 NILM 训练准入要求。

分离端只要求最终得到标准字段名 `aggregate`，不要求使用某一种固定来源方式。

---

## 5. 检测端规范

检测端在分离后的标准信号文件上执行。检测粒度是单文件级，即先对 `House_x/` 目录下的每个 `.h5` 文件分别检测。只有单文件检测通过并完成修复后，才允许进入后续的合并与训练导出阶段。

检测顺序规定如下：

1. 逐个检测 `aggregate_Hx.h5`
2. 逐个检测 `<appliance_name>_Hx.h5`
3. 单文件检测完成后，再进入单文件修复
4. 所有单文件修复完成后，再进行 aggregate 与 appliance 的时间对齐和最终导出

### 5.1 检测维度

#### 5.1.1 结构检测

检查项：

1. `House_x` 目录是否存在。
2. 当前待检测文件是否存在。
3. 文件名是否符合 `<signal_name>_H<house_id>.h5`。
4. 表中是否至少包含 `time` 和 1 个功率列。

#### 5.1.2 时间检测

检查项：

1. 时间列是否可解析为 `datetime`。
2. 时间是否单调递增。
3. 是否存在重复时间戳。
4. 是否存在超长时间空洞。
5. 是否存在异常采样间隔或不规则时间步长。

建议指标：

- `min_timestamp`
- `max_timestamp`
- `row_count`
- `duplicate_timestamp_count`
- `max_gap_seconds`
- `median_gap_seconds`

#### 5.1.3 数值检测

检查项：

1. 是否存在负功率。
2. 是否存在全 0 序列。
3. 是否存在极端尖峰值。
4. 是否存在长时间常数不变段。
5. 是否存在异常缺失比例。

建议指标：

- `nan_ratio`
- `negative_ratio`
- `zero_ratio`
- `constant_segment_count`
- `outlier_count`
- `appliance_gt_aggregate_ratio`

#### 5.1.4 映射检测

检查项：

1. 当前文件中的信号名是否可识别。
2. 文件名中的信号名与文件内部功率字段名是否一致。
3. 是否存在重复信号文件。
4. 是否存在未命名或非法命名文件。

### 5.2 检测结果分级

建议按严重程度分级：

- `INFO`：仅记录统计信息
- `WARN`：存在问题，但允许进入修复端
- `ERROR`：问题严重，禁止进入训练集

建议的阻断条件：

1. 当前文件缺失。
2. 时间列不可解析。
3. 时间序列大面积乱序。
4. 当前文件为空。
5. 修复前缺失比例超过阈值且无法补救。

### 5.3 检测报告示例

```json
{
  "dataset": "REDD",
  "house_id": 1,
  "signal": "dishwasher",
  "status": "WARN",
  "metrics": {
    "row_count": 523441,
    "nan_ratio": 0.012,
    "duplicate_timestamp_count": 34,
    "max_gap_seconds": 180,
    "negative_ratio": 0.0004
  },
  "issues": [
    "duplicate timestamps found",
    "short missing gaps detected"
  ]
}
```

---

## 6. 修复端规范

修复端的目标是把单文件检测中发现的“可修复问题”转换为统一、可用的训练数据。修复粒度同样是单文件级，即先分别修复 `aggregate_Hx.h5` 和各个 `<appliance_name>_Hx.h5`，全部修复完成后再执行合并。

修复策略参考 `NILMFormer` 的数据预处理思路，尤其是以下几类操作：

1. 统一重采样
2. 有限前向填充
3. 小功率归零
4. 非负裁剪与上限裁剪
5. 电器激活阈值过滤
6. NaN 子序列跳过

### 6.1 修复顺序

建议严格按以下顺序执行：

1. 单文件时间排序
2. 单文件去重
3. 单文件重采样到统一频率
4. 单文件缺失值修复
5. 单文件数值裁剪
6. 单文件重采样后完整性复检
7. aggregate 与 appliance 按时间对齐
8. 导出最终训练 CSV

### 6.2 时间修复

规则：

1. 时间戳乱序时，先按时间升序重排。
2. 重复时间戳保留一条，聚合策略默认取均值。
3. 短间隔缺失允许插补。
4. 长间隔缺失不强行补齐，保留空洞并在训练切窗阶段跳过。
5. 若修复后仍存在大时间断点，应按连续时间片段切分，不应将断点两侧数据强行拼接为一个连续序列。

建议阈值：

- 短缺口：`<= 1 min`
- 长缺口：`> 1 min`

### 6.3 缺失值修复

建议策略：

1. 对短缺口使用有限前向填充 `ffill(limit=N)` 或线性插值，且只允许修复 `<= 1 min` 的缺口。
2. 对 aggregate 和 appliance 使用同一时间索引对齐。
3. 对 `> 1 min` 的长缺口不做前向填充，也不做强插值。
4. 重采样后若仍存在明显缺口，应在复检阶段标记出来。
5. 仍含 NaN 的窗口，在训练切窗时剔除。
6. 训练切窗时禁止跨越大时间断点取样。

推荐默认：

- 统一输出频率：`1 kHz`
- 最大前向填充时间：`1 min`

### 6.4 数值修复

规则：

1. 所有功率值小于 0 的样本裁剪到 0。
2. 极小噪声功率可置 0，建议阈值 `< 5W`。
3. 对 aggregate 和 appliance 应设置全局上限裁剪 `clip(lower=0, upper=cutoff)`。
4. 当电器值显著大于总表值时，标记为异常并进入人工复核或阈值修正。

### 6.5 电器状态修复

参考 `NILMFormer` 的 appliance-specific threshold 思路，对关键电器维护最小/最大功率阈值，并据此生成 `status`。

建议为每类电器维护：

- `min_threshold`
- `max_threshold`
- `min_on_duration`
- `min_off_duration`
- `min_activation_time`

用途：

1. 过滤不合理激活片段。
2. 消除毛刺触发。
3. 为后续状态学习模型提供标准标签。

示例：

```yaml
dishwasher:
  min_threshold: 300
  max_threshold: 3000
  min_on_duration: 60
  min_off_duration: 30
  min_activation_time: 60
```

### 6.6 修复后输出规范

修复后的标准信号文件必须满足：

1. 时间索引已对齐到 `1 kHz`
2. 时间单调递增
3. 不存在负功率
4. 不存在非法列名
5. 每个单文件都已单独通过检测与修复
6. aggregate 与 appliance 可按时间直接 join

最终合并规则如下：

1. 不允许先合并、后检测
2. 不允许先合并、后修复
3. 必须先完成单文件检测
4. 必须先完成单文件修复
5. 只有通过修复后的 aggregate 文件和 appliance 文件，才允许按时间 join 并导出最终 CSV

如需进一步给训练代码使用，可继续导出为 CSV：

```text
time,aggregate,dishwasher
2013-05-17 09:35:12,223.45,0.00
2013-05-17 09:35:18,225.12,0.00
2013-05-17 09:35:24,1203.87,981.40
```

---

## 7. 训练接口对接要求

为兼容当前仓库中的 `DatasetManager` 和 `SlidingWindowDataset`，当前默认导出应满足：

1. 每户目录中至少有 `aggregate_Hx.h5` 和目标电器 `xxx_Hx.h5`
2. 两个文件都包含 `time` 列
3. 经过 join 后可重采样到 `1 kHz`
4. 重采样后缺失值应尽量少
5. 导出的 CSV 列顺序固定为：
   - 第 1 列：`time`
   - 第 2 列：`aggregate`
   - 第 3 列：`<appliance>`

---

## 8. 实现总结

1. 输入端：
   - 校验原始数据格式
   - 校验语义映射
   - 校验房屋和通道可读
2. 分离端：
   - 生成 `House_x/<appliance>_Hx.<ext>` 标准信号文件
3. 检测端：
   - 检测缺失、重复、乱序、负值、异常峰值
4. 修复端：
   - 排序
   - 去重
   - 重采样到 `1 kHz`
   - 有限前向填充
   - 非负裁剪
   - 小于 `5W` 归零

其中 `<ext>` 默认推荐为 `.h5`，但通用 pipeline 的核心约束不应是存储格式本身，而应是统一 schema、统一语义和统一训练接口。

---

## 9. 参考依据

本规范与当前仓库现状保持兼容，并参考以下来源：

1. 本仓库现有 `DataSeparator`、`DatasetManager`、`DatasetRegistry`、`SlidingWindowDataset` 的行为约束
2. `NILMFormer` 预处理思路：统一重采样、有限前向填充、小值归零、功率裁剪、按电器阈值生成状态

## 10. 示例说明

当前仓库中已经出现过的 `UKDALE`、`REDD`、`REFIT`、`ECO` 只应被视为 adapter 示例或测试样本，不应被理解为该规范的适用范围上限。
